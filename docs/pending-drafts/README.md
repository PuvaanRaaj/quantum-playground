# Unpublished drafts

These two drafts are **not part of the 60-lesson website** and have not received final Astra approval. They are saved only to make the next session easier. Do not import them into the published catalog yet.

- `newton-laws.json`: final narrative calls the plotted curve a parabola, but the first output (acceleration) is graphed against net force, producing a straight line at fixed mass. Correct that description; displacement is quadratic only when plotted against time.
- `work-energy.json`: model assumptions describe a positive force accelerating from rest, but the force range includes zero. Set its minimum to 0.1 N, or explicitly explain the zero-force value as an algebraic limiting calculation rather than a trajectory from rest.

Apply fixes, run the draft validator, then obtain a new exact-file review from `gpt-6-astra` before publication. Full earlier review notes remain locally in `work/reviews/batch-13.json`.
