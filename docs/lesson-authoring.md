# Lesson contract
Write one JSON file per assigned slug in work/lesson-drafts/SLUG.json. Match existing content/types.ts Topic fields. No edits elsewhere. No commits. New lesson visual is "calculator", with a required model object as below. Existing Topic fields still required. Each lesson >=650 ORIGINAL narrative words in sections alone, 4+ term definitions, history/credit, conceptual explanation, actual reasoning or proof where appropriate, worked numeric example, limits/misconceptions, 2+ authoritative source URLs, a valid 3-option quiz. No filler, motivational slogans, repetitive template prose or artificial word padding. Technical beginner is audience. Sources checked with tools where needed; no invented links. Physical simulation is never evidence establishing a theory. Formal checking is not implemented.

model structure:
{"title":"Plain name","description":"What is computed","inputs":[{"key":"x","label":"Plain quantity","unit":"m","min":1,"max":10,"step":0.1,"initial":3}],"outputs":[{"label":"Quantity","unit":"m²","expression":["pow","x",2]}],"assumptions":"Precisely state idealizations and what this demonstration cannot establish."}
Expression is number, input key string, or array [operator,...args]. Operators +, -, *, /, pow, sqrt, exp, log (natural), sin, cos, abs, min, max, factorial. - / pow use exactly2 args; sqrt exp log sin cos abs factorial exactly1; + * min max 2+. Angles radians. factorial integer0..170 only. NO arbitrary eval, JavaScript, strings as formulas or unsupported operators. Include numeric constants literally (pi=3.141592653589793). Output must remain finite for ENTIRE input ranges and combinations; denominators never0, sqrt argument nonnegative, log positive, factorial integral. 1–3 inputs and 1–3 outputs. First output is graphed against first input. Choose physically meaningful scoped example and sensible units/ranges. A proof-oriented topic can offer numeric examples but explain these do not prove theorem.

category exact assigned. accent violet (quantum), blue (space), amber (math), teal (classical). level Start here / Build intuition / Go deeper. title plain name. subtitle factual one-sentence summary. Avoid slogans. minutes realistic12–18. sources objects {title,url}. Notify parent after each completed file; proceed to next. Do not touch or wait on another writer's files.
