# Optional AI tutor. Configure as server secrets, never NEXT_PUBLIC_ variables.
OPENAI_API_KEY=
# Choose a Responses API model supporting function calling available to your account.
OPENAI_MODEL=
# Generate a random access code of at least 24 characters. Share only with invited testers.
TUTOR_ACCESS_TOKEN=
